import type { BlogPost } from '../../data';

export const post10: BlogPost = {
  id: '10',
  package: 'choi',
  title: '버추얼 스레드는 어떻게 동작하는가 — 마운트, 언마운트, 그리고 이벤트 루프와의 차이',
  excerpt:
    'LLM 호출에 10분이 걸리면 그 요청을 맡은 스레드도 10분간 묶입니다. 버추얼 스레드는 이걸 어떻게 풀까요. 마운트·언마운트 원리부터 FastAPI 이벤트 루프와의 차이, 켜기 전에 알아야 할 문제들까지 정리했습니다.',
  category: 'Backend',
  authorOverride: { name: 'Mr.Choi', role: 'I-Poten Backend Developer', accent: '#0EA5E9' },
  publishedAt: '2026-08-07',
  readingMinutes: 15,
  cover: 'linear-gradient(135deg,#0EA5E9 0%,#1E3A8A 60%,#0F172A 100%)',
  tags: ['Virtual Thread', 'Java 21', 'Spring', 'asyncio', 'Concurrency'],
  body: [
    // ── 도입 ──
    {
      type: 'p',
      text: '기존 방식은 요청 하나가 오면 플랫폼 스레드 하나가 응답까지 전부 담당합니다. 그런데 LLM 호출처럼 응답이 몇 분씩 걸리는 작업이라면? 그 스레드는 그동안 통째로 묶여 있고, 이런 요청이 스레드 최대 개수만큼 들어오면 **서버는 더 이상 요청을 받지 못합니다.**',
    },
    {
      type: 'p',
      text: '억울한 건 서버가 바빠서가 아니라는 점입니다. 스레드들은 전부 **"기다리는 중"이지 "일하는 중"이 아닙니다.** 오늘은 버추얼 스레드가 이걸 어떻게 푸는지, FastAPI 이벤트 루프와는 뭐가 다른지, 그리고 실제로 켤 때 뭘 조심해야 하는지를 봅니다. 이름 규칙 하나만 정하고 시작하겠습니다 — **요청 A / 가상 스레드 Q(대문자) / 실제 스레드 q(소문자)**.',
    },

    { type: 'hr' },

    // ── Part 1 ──
    { type: 'h2', text: 'Part 1. 용어 — 셋은 같은 것이고, 하나만 다르다' },
    {
      type: 'image',
      src: '/blog/choi/10/terms-two-sides.svg',
      alt: '커널 스레드, 플랫폼 스레드, 캐리어 스레드는 실제 실행하는 쪽으로 사실상 같은 실체이고, 가상 스레드만 정보를 담는 힙 위의 자바 객체로 성격이 다르다',
      caption: '왼쪽 셋은 같은 실체의 다른 이름, 오른쪽 하나만 다른 종류입니다.',
    },
    {
      type: 'list',
      items: [
        '**스레드** — 보통 말하는 스레드는 OS 커널 스레드입니다. CPU를 배정받는 진짜 실행 단위.',
        '**플랫폼 스레드** — 자바에서 쓰는 스레드. 커널 스레드와 1:1이라 실체는 같습니다.',
        '**캐리어 스레드** — 플랫폼 스레드인데 지금 가상 스레드를 태우고 있는 상태. 종류가 아니라 **역할 이름**입니다.',
        '**가상 스레드** — 이것만 다릅니다. 실행 능력이 없고, **힙에 놓인 자바 객체**입니다.',
      ],
    },
    {
      type: 'p',
      text: '*"모든 캐리어 스레드는 플랫폼 스레드다"* → 참. *"모든 플랫폼 스레드는 캐리어 스레드다"* → 거짓 (`main`은 가상 스레드를 안 태우고 있으니 캐리어가 아닙니다).',
    },

    { type: 'hr' },

    // ── Part 2 ──
    { type: 'h2', text: 'Part 2. 기존 방식은 왜 스레드를 놓아줄 수 없었나' },
    { type: 'code', language: 'text', text: `A요청 → 과정1 → 과정2 → … → A응답` },
    {
      type: 'p',
      text: '요청 A에 스레드 `q`가 할당되면, **과정2가 10분 대기해도 `q`는 응답까지 계속 점유됩니다.** 왜 못 놓아줄까요? **작업의 진행 상황이 그 스레드의 스택에 박혀 있기 때문입니다.** 어디까지 실행했는지, 지역 변수가 무엇인지가 전부 `q`의 스택에 있으니 놓는 순간 사라집니다.',
    },
    {
      type: 'quote',
      text: '기존 방식이 스레드를 붙잡고 있던 건 일이 많아서가 아니라, 놓으면 어디까지 했는지를 잃어버리기 때문이었습니다.',
    },

    { type: 'hr' },

    // ── Part 3 ──
    { type: 'h2', text: 'Part 3. 마운트와 언마운트' },
    {
      type: 'list',
      items: [
        '**① 할당** — 요청 A에 **가상 스레드 Q**를 할당합니다. Q는 할 일을 정의한 채 스케줄러 큐에 들어갑니다.',
        '**② 마운트** — 스케줄러가 놀고 있는 캐리어 `q`를 골라 Q를 그 위에 올립니다. 이제 `q`가 실제로 일합니다.',
        '**③ 언마운트** — 대기가 발생하면 **Q는 진행 정보를 힙에 저장하고 `q`를 반납합니다.** 이 순간부터 `q`는 자유입니다.',
        '**④ 재마운트** — 응답이 오면 다시 캐리어를 받습니다. 이때 캐리어는 `q`일 수도, **다른 스레드일 수도 있습니다.**',
      ],
    },
    {
      type: 'image',
      src: '/blog/choi/10/ten-min-comparison.svg',
      alt: '기존 방식은 스레드들이 10분 대기 동안 점유되어 새 요청을 거절하지만, 버추얼 스레드는 대기 정보를 힙에 저장하고 캐리어 스레드가 같은 10분 동안 다른 요청들을 처리한다',
      caption: '같은 10분 — 기존은 점유, 버추얼 스레드는 힙에 맡기고 캐리어가 다른 요청을 처리합니다.',
    },
    {
      type: 'p',
      text: '오해하기 쉬운데, **요청 하나의 처리 시간은 여전히 10분입니다.** 달라진 건 그 10분 동안 서버가 마비되느냐, 다른 요청을 계속 받느냐입니다.',
    },

    { type: 'hr' },

    // ── Part 4 ──
    { type: 'h2', text: 'Part 4. 왜 다른 스레드가 이어받아도 되는가' },
    {
      type: 'quote',
      text: '가상 스레드는 실행할 작업과 진행 상황을 저장한 자바 객체입니다. 캐리어 스레드는 그것을 읽어서 실제로 동작시키는 스레드일 뿐입니다.',
    },
    {
      type: 'p',
      text: '정보가 전부 가상 스레드 쪽에 있으니 실행자는 누구든 상관없습니다. 캐리어는 아무 정보도 없는 빈 껍데기니까요. **AI와 작업할 때를 떠올리면 쉽습니다.** A 세션에서 작업하다 진행 상황을 `.md`로 정리해두면, 다른 세션을 열어도 그 파일을 읽고 이어갈 수 있죠. **맥락이 세션이 아니라 파일에 있기 때문**입니다.',
    },
    {
      type: 'image',
      src: '/blog/choi/10/md-handoff-analogy.svg',
      alt: 'AI 세션 A에서 작업하다 진행상황을 md 파일로 정리하면 다른 세션 B가 읽고 이어서 작업한다. md 파일이 가상 스레드, AI 세션이 캐리어 스레드에 해당한다',
      caption: '가상 스레드가 그 `.md` 파일, 캐리어 스레드가 세션입니다.',
    },
    {
      type: 'code',
      language: 'text',
      text: `기존:  정보가 스레드에 박힘  →  스레드를 놓으면 정보도 잃음  →  못 놓음
가상:  정보를 힙으로 분리    →  스레드를 놓아도 정보는 남음  →  놓을 수 있음`,
    },

    { type: 'hr' },

    // ── Part 5 ──
    { type: 'h2', text: 'Part 5. 가상 스레드는 세 장소를 돌아다닌다' },
    {
      type: 'image',
      src: '/blog/choi/10/three-places-cycle.svg',
      alt: '가상 스레드는 스케줄러 큐에서 대기하다 캐리어 스레드에 마운트되어 실행되고, 대기가 발생하면 언마운트되어 힙에 저장되며, 응답이 오면 언파크로 다시 큐에 등록되는 순환 구조',
      caption: '큐 → (마운트) → 캐리어 → (언마운트) → 힙 → (언파크) → 다시 큐.',
    },
    {
      type: 'p',
      text: '한 가지만 짚으면, **대기 중인 가상 스레드는 큐에 없습니다.** 큐에 남아 있으면 캐리어가 계속 꺼내서 "아직이야?" 하고 확인해야 하니까요. 응답이 도착해야 큐로 돌아오는데, 이 복귀를 **언파크(unpark)** 라고 합니다.',
    },

    { type: 'hr' },

    // ── Part 6 ──
    { type: 'h2', text: 'Part 6. FastAPI 이벤트 루프와 무엇이 다른가' },
    {
      type: 'p',
      text: '"이거 FastAPI가 하는 거랑 똑같은데?" **맞습니다.** 밑바닥 원리는 거의 같습니다. 둘 다 *"기다릴 때는 상태만 저장하고 실제 스레드를 놓아준다"* 는 아이디어이고, 용어만 다릅니다.',
    },
    {
      type: 'table',
      headers: ['자바', '파이썬(asyncio)'],
      rows: [
        ['가상 스레드', '코루틴'],
        ['캐리어 스레드', '이벤트 루프 스레드'],
        ['언마운트', '`await`에서 양보'],
        ['스케줄러 큐', '이벤트 루프 태스크 큐'],
      ],
    },
    { type: 'h3', text: '그럼 FastAPI와 WebFlux는 같은 건가?' },
    {
      type: 'p',
      text: '둘 다 "이벤트 루프 기반"이라 불리지만, **개발자가 쓰는 코드 모양이 완전히 다릅니다.**',
    },
    {
      type: 'code',
      language: 'java',
      text: `// WebFlux — 연산자를 이어 붙인 파이프라인. 순차 코드가 아니다.
return userRepo.findById(id)
    .flatMap(user -> orderRepo.findByUser(user))
    .map(orders -> toResponse(orders));`,
    },
    {
      type: 'code',
      language: 'python',
      text: `# FastAPI — 위에서 아래로 읽히는 순차 코드. await 표시만 붙는다.
user   = await repo.find(id)
orders = await order_repo.find_by_user(user)
return to_response(orders)`,
    },
    {
      type: 'p',
      text: 'WebFlux는 코드를 **콜백 체인으로 조립**해서 흐름이 조각나고 스택 트레이스도 끊깁니다. FastAPI의 코루틴은 **평범한 순차 코드**로, `await`에서 멈췄다 그 자리에서 이어집니다. 그래서 셋은 이런 스펙트럼이 됩니다.',
    },
    {
      type: 'code',
      language: 'text',
      text: `WebFlux  ────────  FastAPI  ────────  가상 스레드
콜백 체인          순차 + await 표시      순차, 표시조차 없음`,
    },
    {
      type: 'p',
      text: '차이가 코드에서 **"어디까지 물드는가"**로 드러납니다. FastAPI는 함수에 `async`만 붙고 데이터 타입은 평범합니다. **WebFlux는 타입 자체가 `Mono`·`Flux`로 바뀌고**, 컨트롤러부터 DB 드라이버까지 스택 전체가 논블로킹이어야 합니다. 중간에 블로킹 코드가 한 줄만 섞여도 이벤트 루프가 통째로 막히고요.',
    },
    {
      type: 'p',
      text: '아래는 이 문단들에 나온 단어 정리입니다. 헷갈리면 여기만 보셔도 됩니다.',
    },
    {
      type: 'table',
      headers: ['단어', '쉬운 뜻'],
      rows: [
        ['이벤트 루프', '준비된 일부터 골라 처리하는 반복문'],
        ['코루틴', '멈췄다가 그 자리에서 이어지는 함수 (책갈피)'],
        ['`Mono` · `Flux`', '곧 올 값 1개 / 여러 개를 담는 상자'],
        ['배압(backpressure)', '받는 쪽이 "속도 좀 늦춰줘"라고 말하는 규약'],
        ['논블로킹', '기다리는 동안 스레드를 붙잡지 않는 것'],
        ['JDBC · R2DBC', '멈춰서 기다리는 DB 접속 / 안 멈추는 DB 접속'],
      ],
    },
    { type: 'h3', text: '그럼 버추얼 스레드도 이벤트 기반인가요?' },
    {
      type: 'p',
      text: '**밑바닥은 그렇습니다.** 가상 스레드가 DB 응답을 기다릴 때 JVM이 무작정 확인하는 게 아니라, OS의 이벤트 알림(`epoll`)에 "이 소켓 준비되면 알려줘"라고 등록해둡니다. 그리고 JVM 내부의 감시 스레드(Poller)가 신호를 받아 해당 가상 스레드를 깨우죠(언파크). **이벤트 루프가 없는 게 아니라, JVM 안에 숨어 있는 것**입니다.',
    },
    {
      type: 'image',
      src: '/blog/choi/10/where-is-the-loop.svg',
      alt: 'WebFlux는 이벤트 루프와 리액티브 규약이 개발자 코드까지 올라오고, FastAPI는 await 표시만 남으며, 가상 스레드는 루프가 JVM 안에 완전히 숨는다. 셋 다 밑바닥은 같은 OS 이벤트 기능 위에서 돈다',
      caption: '셋의 차이는 "이벤트 루프가 있느냐"가 아니라 "그게 내 코드까지 올라오느냐"입니다.',
    },
    {
      type: 'p',
      text: '그래서 정확한 구분은 이렇습니다. 셋 다 **런타임은 이벤트 기반**입니다. 다른 건 **프로그래밍 모델** — 이벤트 루프의 존재를 개발자가 얼마나 의식해야 하느냐죠. WebFlux는 코드가 그 규약을 따라야 하고, FastAPI는 `await` 표시까지만, 가상 스레드는 아예 몰라도 됩니다.',
    },
    { type: 'h3', text: '그럼 WebFlux는 왜 쓰나 — 잘하는 것들' },
    {
      type: 'p',
      text: '여기까지만 보면 WebFlux가 손해만 보는 것 같지만, 그렇지 않습니다. **가상 스레드가 못 하는 걸 WebFlux는 합니다.**',
    },
    {
      type: 'list',
      items: [
        '**배압(backpressure)** — 가장 큰 무기입니다. 받는 쪽이 감당 못 하면 **보내는 쪽을 늦춥니다.** 가상 스레드에는 이 규약이 없어서 요청이 무한정 쌓이고, 그게 바로 Part 8 ④의 OOM 시나리오입니다. WebFlux는 그 문제를 프로토콜 차원에서 막습니다.',
        '**끝없이 흐르는 데이터** — SSE·WebSocket, 실시간 이벤트 파이프라인처럼 **끝이 정해지지 않은 스트림**이 `Flux`의 본래 목적입니다. 가상 스레드는 "요청 하나 → 응답 하나"에 맞춰진 모델이라 여기선 어색합니다.',
        '**연결은 많은데 대부분 놀 때** — 요청당 스택이 없어서 **연결 하나당 메모리가 훨씬 적습니다.** 수십만 개 연결을 열어두고 가끔씩만 데이터가 오가는 게이트웨이·푸시 서버라면 여전히 WebFlux가 유리합니다. (Spring Cloud Gateway가 WebFlux 기반인 이유입니다.)',
        '**재시도·타임아웃·병렬 호출을 선언적으로** — `retry`, `timeout`, `zip` 같은 연산자로 한 줄이면 되는 것들을, 가상 스레드에서는 직접 짜야 합니다.',
      ],
    },
    {
      type: 'quote',
      text: '가상 스레드는 "요청 하나를 편하게 처리하는" 문제를 풀었고, WebFlux는 "끝없이 흐르는 데이터를 감당 가능한 속도로 다루는" 문제를 풀었습니다. 애초에 겨냥한 문제가 다릅니다.',
    },
    {
      type: 'p',
      text: '정리하면 — **평범한 요청·응답 API라면 가상 스레드**가 훨씬 편하고 성능도 비슷합니다. 반면 **스트리밍, 배압, 수십만 유휴 연결**이 필요하다면 그건 WebFlux의 홈그라운드입니다. "WebFlux는 이제 필요 없다"가 아니라 **"WebFlux를 써야 할 이유가 더 좁고 뚜렷해졌다"** 가 맞는 표현입니다.',
    },
    { type: 'h3', text: '남은 두 가지 차이' },
    {
      type: 'p',
      text: '**① 양보 지점을 직접 찍느냐** — FastAPI는 `await`를 개발자가 씁니다. 그래서 `async` 함수를 부르려면 호출하는 쪽도 `async`여야 하는 **함수 색깔 문제**가 생기고, 라이브러리도 async 전용 버전이 필요합니다. 자바는 표시가 없어 기존 코드와 라이브러리를 그대로 씁니다. (다만 *모든* 대기가 자동은 아닙니다 — 예외는 Part 8에서.)',
    },
    {
      type: 'image',
      src: '/blog/choi/10/fastapi-vs-vt.svg',
      alt: 'FastAPI는 코루틴들이 이벤트 루프 하나에서 한 번에 하나씩 실행되어 코어를 쓰려면 프로세스를 복제해야 하고, 자바는 가상 스레드들이 코어 수만큼의 캐리어 스레드에서 한 프로세스 안에 병렬 실행된다',
      caption: '루프 1개(프로세스 복제로 확장) vs 캐리어 여러 개(한 프로세스 안에서 병렬).',
    },
    {
      type: 'p',
      text: '**② 루프가 몇 개냐** — asyncio는 프로세스당 하나라 코어를 다 쓰려면 `uvicorn --workers 8`처럼 프로세스를 복제합니다. 자바는 캐리어가 코어 수만큼 있어 한 프로세스 안에서 병렬 실행되고 힙도 공유합니다. **실수했을 때 대가도 다릅니다** — `async def` 안에서 동기 라이브러리를 부르면 프로세스 전체가 멈추지만, 자바에서 피닝이 나면 캐리어 하나만 묶입니다.',
    },
    {
      type: 'quote',
      text: 'FastAPI는 "비동기를 쓰려면 코드를 비동기로 바꾸라"고 하고, 가상 스레드는 "동기 코드 그대로 두면 우리가 알아서 돌려주겠다"고 합니다.',
    },

    { type: 'hr' },

    // ── Part 7 ──
    { type: 'h2', text: 'Part 7. 적용은 설정 한 줄' },
    {
      type: 'code',
      language: 'yaml',
      text: `spring:
  threads:
    virtual:
      enabled: true`,
    },
    {
      type: 'p',
      text: 'Spring Boot 3.2 이상이면 코드는 그대로 두고 이 한 줄이면 켜집니다. 하나만 미리 짚으면 — **가상 스레드는 풀에 담지 않습니다.** 생성 비용이 거의 없어 요청마다 새로 만드는 게 정석이고, 풀에 넣으면 개수 제한이 다시 생겨 이점이 사라집니다. 다만 "켜면 다 된다"는 뜻은 아닙니다.',
    },

    { type: 'hr' },

    // ── Part 8 ──
    { type: 'h2', text: 'Part 8. 켜기 전에 알아야 할 다섯 가지' },
    { type: 'h3', text: '① 병목은 사라지지 않고 다음 약한 곳으로 옮겨간다' },
    {
      type: 'p',
      text: '스레드 200개 제한은 병목인 동시에 **하위 시스템을 지켜주는 안전장치**였습니다. 요청이 아무리 몰려도 DB에는 최대 200개만 도달했으니까요.',
    },
    {
      type: 'image',
      src: '/blog/choi/10/bottleneck-shift.svg',
      alt: '기존에는 스레드 200개 제한이 병목이자 안전장치였지만, 버추얼 스레드로 제한이 풀리면 요청 5000개가 그대로 DB 커넥션 풀 20개로 몰려 새 병목이 된다',
      caption: '스레드 병목을 없애면, 그 부하가 고스란히 다음 단계로 넘어갑니다.',
    },
    {
      type: 'p',
      text: '켜는 순간 그 장치가 사라져 요청 5,000개가 **커넥션 20개짜리 DB 앞에 한꺼번에 도착**합니다. 외부 API는 `429`가 쏟아지고요. 해결은 **없앤 안전장치를 명시적으로 다시 거는 것**입니다.',
    },
    {
      type: 'code',
      language: 'java',
      text: `// 외부 LLM 호출은 동시 50건까지만
private final Semaphore llmLimit = new Semaphore(50);

llmLimit.acquire();
try { return llmClient.call(q); }
finally { llmLimit.release(); }`,
    },
    { type: 'h3', text: '② 피닝 — 정작 못 고치는 건 라이브러리 안쪽' },
    {
      type: 'p',
      text: '`synchronized` 블록 안에서 블로킹되면 가상 스레드가 내려오지 못하고 **캐리어째 묶입니다.** 캐리어는 코어 수만큼뿐이라 오히려 더 심하게 막힐 수 있습니다. **Java 24(JEP 491)에서 대부분 해결됐지만**, 그 이전 버전에서 진짜 문제는 — 내 코드의 `synchronized`는 `ReentrantLock`으로 바꿔도 **라이브러리 내부는 못 고친다**는 점입니다.',
    },
    {
      type: 'code',
      language: 'bash',
      text: `# Java 21 ~ 23 — 피닝 지점의 스택 트레이스
-Djdk.tracePinnedThreads=full

# Java 24 이상 — 위 옵션은 제거됐고 JFR 이벤트로 확인
jdk.VirtualThreadPinned`,
    },
    { type: 'h3', text: '③ 모든 기다림이 언마운트되지는 않는다' },
    {
      type: 'p',
      text: '"동기 코드를 쓰면 JDK가 알아서 처리한다"는 **JDK 내부에서 다시 작성된 지점에 한해서** 참입니다. 소켓 통신은 되지만 **`java.io` 파일 입출력**, **네이티브(JNI) 호출**, **`Object.wait()`** 는 언마운트되지 않고 캐리어를 붙잡습니다. 그래서 이득이 확실한 건 **네트워크 대기** — DB 조회와 외부 API 호출 쪽입니다.',
    },
    { type: 'h3', text: '④ 동시 요청이 그대로 메모리가 된다 — OOM 시나리오' },
    {
      type: 'p',
      text: '①을 메모리 관점에서 보면 더 무섭습니다. 스레드 200개 시절엔 **그 상한이 곧 메모리 상한**이었습니다. 이제 상한이 사라지면 `동시 진행 요청 수 × 요청 하나가 붙잡는 메모리`가 그대로 힙이 됩니다. 여기서 흔한 오해가 있는데, 가상 스레드 **객체**가 수백 바이트인 것이지 **요청 하나**가 그런 게 아닙니다. 스택·HTTP 버퍼·조회한 엔티티·`ThreadLocal`까지 전부 붙잡고 있습니다.',
    },
    {
      type: 'code',
      language: 'text',
      text: `초당 100건, 한 건이 10분(600초) 걸린다면
  → 동시에 살아 있는 요청 = 100 × 600 = 60,000건
  → 하나가 20KB만 붙잡아도 약 1.2GB

힙이 2GB인 서버라면? 여기서 끝납니다.`,
    },
    {
      type: 'p',
      text: '빨리 끝나는 요청은 안 쌓입니다. 문제는 **오래 기다리는 요청**이고, 하필 그게 버추얼 스레드를 도입하는 이유죠. 방어선은 셋입니다 — **① 외부 호출에 타임아웃을 반드시 걸고**(가장 중요합니다. 안 끝나는 요청은 어떤 상한도 결국 채웁니다), **② `Semaphore`로 동시 실행을 제한하고**, **③ `server.tomcat.max-connections`를 확인합니다.** 가상 스레드를 켜면 `threads.max`는 무의미해지지만 이 값(기본 8,192)은 그대로 살아 있는 마지막 안전선입니다.',
    },
    { type: 'h3', text: '⑤ 관측 도구가 아직 못 따라온다' },
    {
      type: 'p',
      text: '장애 때 늘 쓰던 `jstack`에 **가상 스레드는 보이지 않습니다.** 수십만 개를 기존 포맷으로 찍을 수 없기 때문인데, 진짜 문제는 도구보다 **습관**입니다. 급할 때 손이 먼저 가는 게 `jstack`이라, 정작 장애 상황에서 "왜 아무것도 안 보이지?"로 시간을 날리기 쉽습니다.',
    },
    {
      type: 'code',
      language: 'bash',
      text: `jcmd <pid> Thread.dump_to_file -format=json dump.json`,
    },
    {
      type: 'quote',
      text: '버추얼 스레드는 스레드 병목만 없앱니다. 시스템 전체를 빠르게 만들지 않고, 병목을 다음 가장 약한 곳으로 옮길 뿐입니다.',
    },

    { type: 'hr' },

    // ── Part 9 ──
    { type: 'h2', text: 'Part 9. 숫자로 보면' },
    {
      type: 'p',
      text: '저희가 측정한 값이 아니라 **공개된 제3자 벤치마크**입니다. 먼저 논쟁의 여지가 없는 것 — 스레드 한 개의 비용은 구조에서 나오는 차이라 어디서 재도 같습니다.',
    },
    {
      type: 'table',
      headers: ['', '플랫폼 스레드', '가상 스레드'],
      rows: [
        ['한 개당 메모리', '스택 약 1MB 예약', '힙에 수백 바이트 ~ 수 KB'],
        ['현실적 최대 개수', '수천 개', '수십만 ~ 수백만 개'],
        ['생성 비용', 'OS 호출 — 그래서 풀링한다', '거의 없음 — 그래서 풀링하지 않는다'],
      ],
    },
    {
      type: 'p',
      text: '`ankurm.com`의 벤치마크입니다. **JDK 21 · Spring Boot 3.4 · 8코어 · 동시 연결 500**에서 DB가 20ms 걸리는 요청을 처리한 결과입니다.',
    },
    {
      type: 'image',
      src: '/blog/choi/10/benchmark-io.svg',
      alt: 'DB 20ms 대기 워크로드에서 플랫폼 스레드는 초당 340건에 p99 1240밀리초, 가상 스레드는 4180건에 41밀리초, WebFlux는 4520건에 37밀리초',
      caption: '같은 하드웨어, 같은 로직 — 스레드 모델만 바꾼 결과입니다.',
    },
    {
      type: 'p',
      text: '외부 API 150ms 경우도 결이 같습니다(133 / 960 / 1,010 req/s). 반면 **CPU 연산만 하는 워크로드**에서는 2,140 / 2,110 / 1,980으로 사실상 동일합니다. Part 8 ③에서 말한 "CPU엔 효과 없다"가 그대로 확인되는 셈이죠.',
    },
    {
      type: 'p',
      text: '더 큰 부하를 다룬 `chrisgleissner/loom-webflux-benchmarks`에서는 **6만 동시 사용자** 기준으로 가상 스레드가 중앙값 100ms·GC 12초, WebFlux가 1,174ms·GC 108초를 기록했습니다. 다만 같은 저장소의 다른 시나리오에서는 WebFlux가 근소하게 이깁니다. 여기서 실무적으로 중요한 발견은 따로 있는데 — **Tomcat 위의 가상 스레드는 2만 명이 한꺼번에 몰리자 연결 거부가 99% 넘게 발생했습니다.** 처리 능력이 아니라 TCP 연결을 받아들이는 속도가 병목이었고, Netty는 같은 부하를 문제없이 받았습니다. Part 8 ①의 실제 사례입니다.',
    },
    {
      type: 'p',
      text: '정직하게 덧붙이면 **공개 벤치마크들의 결론은 서로 다릅니다.** 워크로드·서버·JDK 버전·부하 패턴에 따라 순위가 바뀝니다. 그래도 공통된 두 가지는 있습니다 — **① I/O 대기가 긴 워크로드에서 플랫폼 스레드와의 격차는 확실하고 큽니다. ② 가상 스레드와 WebFlux의 처리량 차이는 대개 오차 범위이고, 갈리는 건 코드 복잡도와 디버깅 난이도입니다.**',
    },
    {
      type: 'quote',
      text: '남의 벤치마크는 방향만 알려줍니다. 우리 서비스의 숫자는 우리가 재야 합니다.',
    },
    {
      type: 'p',
      text: '설정 한 줄로 켜고 끌 수 있으니 같은 부하를 두 번 걸어 비교하면 됩니다. 이때 **처리량과 p99만 보면 안 됩니다** — DB 커넥션 풀 대기 시간, 연결 거부·에러율, 힙과 GC 시간, 피닝 발생 여부를 함께 봐야 병목이 어디로 옮겨갔는지 보입니다.',
    },

    { type: 'hr' },

    // ── 질문 있습니다 ──
    { type: 'h2', text: '질문 있습니다' },
    {
      type: 'p',
      text: '**Q. (스레드 기반 대비) 요청이 더 빨라지나요?** 조건에 따라 다릅니다. **서버가 한가할 때는 똑같습니다** — DB가 3초 걸리면 어느 쪽이든 3초입니다. **하지만 부하가 걸리면 달라집니다.** 스레드 기반은 200개가 다 차는 순간부터 새 요청이 대기열에서 기다려 체감 응답이 폭증하지만, 버추얼 스레드는 그 구간이 없습니다. 위 벤치마크의 p99 1,240ms 대 41ms가 그 차이입니다. 한 줄로 — **작업이 빨라지는 게 아니라 줄 서던 시간이 사라지는 것**입니다.',
    },
    {
      type: 'p',
      text: '**Q. (WebFlux처럼) 요청이 여러 조각으로 쪼개지나요?** 아닙니다. WebFlux는 요청 하나가 연산자 단위로 쪼개져 조각마다 다른 스레드가 이어받지만, 버추얼 스레드는 **가상 스레드 하나가 처음부터 끝까지** 처리합니다. 나눠지는 건 요청이 아니라 **캐리어 스레드의 시간**입니다.',
    },
    {
      type: 'p',
      text: '**Q. 그럼 WebFlux는 이제 필요 없나요?** 단순히 동시 처리량이 부족했던 경우라면 가상 스레드로 충분합니다. 다만 배압이 필요한 스트리밍, SSE·WebSocket 같은 지속 연결처럼 **리액티브 스트림의 의미론 자체가 필요한 경우**에는 여전히 WebFlux가 맞습니다. WebFlux는 아키텍처를 바꿔서 풀었고, 가상 스레드는 아키텍처를 안 바꾸고 풀었습니다.',
    },
    {
      type: 'p',
      text: '**Q. `ThreadLocal`은 깨지지 않나요?** 가상 스레드에 붙어 함께 이동하므로 동작은 안전합니다. 다만 스레드 수만큼 늘어나니 무거운 객체를 담아두면 메모리 압박이 됩니다. 대안은 `ScopedValue`입니다.',
    },

    { type: 'hr' },

    // ── 마치며 ──
    { type: 'h2', text: '마치며' },
    {
      type: 'code',
      language: 'text',
      text: `요청은 대부분의 시간을 기다리는 데 쓴다
  → 그런데 기존 스레드는 기다리는 동안에도 비싼 OS 스레드를 붙잡는다
  → 기다릴 땐 진행 상황만 저장하고 자리를 비켜주면 된다
  → 그러려면 스택을 저장하고 복원할 수 있어야 하고, 그게 Continuation이다
  → 그걸 스레드처럼 쓰게 감싼 것이 가상 스레드다`,
    },
    {
      type: 'quote',
      text: '비동기의 성능을, 동기 코드의 가독성으로.',
    },
    {
      type: 'p',
      text: '결국 버추얼 스레드가 한 일은 **맥락을 스레드에서 떼어내 힙으로 옮긴 것** 하나입니다. AI 세션의 맥락을 `.md`로 빼두면 어느 세션에서든 이어갈 수 있듯이, 진행 상황을 객체로 빼두면 어느 캐리어든 이어받을 수 있습니다. 그 단순한 분리 하나로 10분 대기가 더 이상 서버를 멈추지 못하게 됐습니다.',
    },
    {
      type: 'p',
      text: '부족한 글 읽어주셔서 감사합니다. 피드백이나 다른 관점은 언제든 환영합니다.',
    },
  ],
};
