import type { BlogPost } from '../../data';

export const post04: BlogPost = {
  id: '4',
  package: 'choi',
  project: 'i-poten',
  title: 'Docker 이야기 — 도커란 무엇인가',
  excerpt:
    'Docker가 무엇인지 가장 쉬운 비유에서 시작해, 자주 등장하는 VM과 커널이 정확히 무엇인지, 이미지를 작고 빠르게 만드는 법, 직접 설치와의 장단점 비교, 그리고 실제 배포에서 만난 이야기들까지 하나씩 풀어봤습니다. I-Poten의 네 런타임 스택을 `docker compose up` 한 줄로 묶은 과정을 곁들입니다.',
  category: 'Backend',
  authorOverride: { name: 'Mr.Choi', role: 'I-Poten Backend Developer', accent: '#0EA5E9' },
  publishedAt: '2026-07-10',
  readingMinutes: 20,
  cover: 'linear-gradient(135deg,#0EA5E9 0%,#1E3A8A 60%,#0F172A 100%)',
  tags: ['Docker', 'Docker Compose', 'Spring Boot', 'DevOps', 'Kubernetes'],
  body: [
    // ── 도입 훅 ──
    {
      type: 'p',
      text: '"제 컴퓨터에선 분명 됐는데…" 개발자라면 누구나 한 번쯤 해본 말이고, 하필 배포 직전에 터지는 말이기도 합니다. 프로그램을 만든 환경과 실행하는 환경이 조금만 달라도 — 라이브러리 버전이 다르거나, OS가 다르거나, 환경 변수 하나가 빠졌거나 — 멀쩡하던 게 남의 컴퓨터에선 멈춥니다. 원인은 늘 사소하고, 발견은 늘 뒤늦습니다.',
    },
    {
      type: 'p',
      text: 'I-Poten은 이 문제가 유난히 크게 다가오는 프로젝트였습니다. React 프론트엔드는 Node 20, Spring Boot 백엔드는 JDK 21, Python AI 서비스는 3.12, 데이터베이스는 PostgreSQL 16 — 네 가지 런타임이 섞여 있고, 이걸 팀원 모두가 똑같이 실행하고 운영 서버에도 똑같이 올려야 했으니까요.',
    },
    {
      type: 'p',
      text: '이 문제를 정면으로 없애는 도구가 Docker입니다. 저는 Docker를 꽤 오래 "환경째로 담아 어디서든 돌려주는 마법 상자" 정도로만 쓰다가, 이번에 기초부터 다시 이해하게 됐습니다. 컨테이너가 정확히 무엇인지, 왜 그렇게 가벼운지, 그 가벼움이 무엇을 대가로 하는지.',
    },
    {
      type: 'p',
      text: '오늘은 그 이야기를 처음부터 하나씩 풀어보려 합니다. 컨테이너가 무엇인지에서 시작해, 자주 같이 등장하는 **VM과 커널**이 무엇인지 짚고, 이미지를 작고 빠르게 만드는 법과 실제 배포에서 제가 밟은 함정들, 그리고 다섯 서비스를 명령 한 줄로 묶은 과정까지. 순서대로 따라오시면 됩니다.',
    },

    { type: 'hr' },

    // ── Part 1 ──
    { type: 'h2', text: 'Part 1. Docker란 무엇인가 — 가장 쉬운 설명부터' },
    {
      type: 'p',
      text: '아주 단순하게 말하면, **Docker는 프로그램을 "실행에 필요한 모든 것과 함께 통째로 포장"해서 어떤 컴퓨터에서든 똑같이 돌아가게 해주는 도구**입니다. 앞에서 말한 "내 컴퓨터에선 됐는데"가 생기는 이유가 환경 차이였다면, Docker는 아예 환경을 프로그램과 함께 싸서 옮겨버립니다.',
    },
    {
      type: 'p',
      text: '이름이 Docker(부두 노동자)이고 로고가 컨테이너를 실은 고래인 데는 이유가 있습니다. 화물 컨테이너의 힘은 **"안에 무엇이 들었든 규격이 같아서 어떤 배·트럭·크레인에나 실린다"** 는 표준화에 있죠. 예전엔 짐마다 크기와 포장이 제각각이라 옮길 때마다 고생했지만, 규격 상자에 담는 순간 운송이 단순해졌습니다. Docker는 그 발상을 소프트웨어로 가져왔습니다.',
    },
    { type: 'h3', text: '이미지와 컨테이너 — 붕어빵 틀과 붕어빵' },
    {
      type: 'p',
      text: '딱 두 단어만 구분하면 나머지가 전부 명확해집니다. **이미지(image)** 는 설계도, 혹은 붕어빵 틀입니다. 앱과 그 환경을 굳혀놓은, 한번 만들면 변하지 않는 파일이죠. **컨테이너(container)** 는 그 틀로 찍어낸 붕어빵, 즉 이미지를 실제로 실행한 인스턴스입니다. 틀 하나로 붕어빵을 여러 개 찍듯, 이미지 하나로 컨테이너를 여러 개 띄울 수 있습니다. 이게 나중에 "AI 서비스만 3개로 늘려라" 같은 확장의 기반이 됩니다.',
    },
    {
      type: 'code',
      language: 'text',
      text: `Dockerfile  (무엇을 어떻게 설치·실행할지 기록)
   │  docker build
   ▼
이미지      (변하지 않는 배포 단위, 레지스트리에 공유)
   │  docker run
   ▼
컨테이너    (실제 실행, 이미지 하나로 여러 개 찍어낸다)`,
    },
    {
      type: 'p',
      text: '여기서 진짜 핵심은 **환경이 문서가 아니라 코드가 된다**는 점입니다. "Python 3.12 깔고 requirements.txt 보고 설치하세요"라고 README에 적는 대신, Dockerfile 한 장이 그 절차를 실행 가능한 형태로 못 박습니다. 이렇게 만든 이미지를 레지스트리(Docker Hub, GitHub Container Registry 등)에 올려두면, 팀원은 명령 한 줄로 내려받아 똑같이 실행합니다. "내 컴퓨터에선 됐는데"가 구조적으로 사라지는 거죠.',
    },

    { type: 'hr' },

    // ── Part 2 ──
    { type: 'h2', text: 'Part 2. "그거 가상 머신 아니야?" — VM과 커널 이야기' },
    {
      type: 'p',
      text: 'Docker를 설명하면 거의 항상 따라 나오는 질문이 있습니다. **"그거 결국 가상 머신(VM) 아니야?"** 좋은 질문입니다. 그런데 이 질문에 제대로 답하려면 VM이 무엇인지, 그리고 그 설명에서 자꾸 튀어나오는 **커널**이 무엇인지부터 짚어야 합니다. 잠깐 둘러 가보겠습니다. 이 우회로가 사실 Docker의 정체를 이해하는 지름길이거든요.',
    },
    { type: 'h3', text: '가상 머신(VM)이란 — 소프트웨어로 만든 가짜 컴퓨터' },
    {
      type: 'p',
      text: 'VM은 **소프트웨어로 흉내 낸 "가짜 컴퓨터" 한 대**입니다. 진짜 컴퓨터 위에서, 마치 별도의 물리 컴퓨터가 여러 대 있는 것처럼 동작하죠. 이를 가능하게 하는 게 **하이퍼바이저(hypervisor)** 입니다. 하이퍼바이저는 진짜 하드웨어의 CPU·메모리·디스크를 쪼개 각 VM에게 "이건 네 하드웨어야" 하고 나눠 줍니다. 그러면 각 VM은 자기가 진짜 컴퓨터 위에 있다고 믿고, 그 안에 **자기만의 완전한 OS를 통째로** 깔아 돌립니다.',
    },
    {
      type: 'p',
      text: '특징은 명확합니다. 각 VM이 진짜 OS를 통째로 품으니 **격리는 아주 강하지만 무겁습니다.** 부팅에 수십 초, 용량은 수 GB, 한 서버에 여러 대 띄우기도 버겁죠. "완전한 OS를 품는다"에서 핵심이 바로 다음에 나올 커널입니다.',
    },
    { type: 'h3', text: '커널이란 — 하드웨어와 앱 사이의 중재자' },
    {
      type: 'p',
      text: '**커널(kernel)은 운영체제(OS)의 심장**입니다. 하는 일을 한마디로 하면 "하드웨어와 앱 사이의 중재자"죠. 우리가 만든 앱은 CPU·메모리·디스크·네트워크를 직접 만지지 못합니다. 대신 커널에게 부탁합니다 — "이 파일 읽어줘", "메모리 좀 줘", "이 데이터 네트워크로 보내줘". 이 요청 통로를 **시스템 콜(system call)** 이라고 부릅니다.',
    },
    {
      type: 'code',
      language: 'text',
      text: `        앱 (프로세스)
          │  시스템 콜 ("파일 읽어줘", "메모리 줘")
          ▼
        커널   ← 실제로 하드웨어를 조작
          │
          ▼
   CPU · 메모리 · 디스크 · 네트워크 (하드웨어)`,
    },
    {
      type: 'p',
      text: '프로세스 스케줄링, 메모리 관리, 파일시스템, 네트워크, 권한 통제 — 컴퓨터의 밑바닥 살림은 전부 커널이 합니다. 그래서 **"OS = 커널 + 그 위의 도구들(셸·라이브러리·유틸리티)"** 이라고 보면 됩니다. 우분투와 페도라는 겉모습은 다르지만 둘 다 리눅스 커널을 씁니다. 앞서 VM이 "완전한 OS를 통째로 품는다"고 한 건, 곧 **이 커널까지 자기 안에 하나 더 올린다**는 뜻이었습니다.',
    },
    { type: 'h3', text: '그래서 Docker는? — 커널을 공유한다' },
    {
      type: 'p',
      text: '이제 처음 질문에 답할 수 있습니다. VM은 게스트마다 커널을 통째로 올리지만, **Docker 컨테이너는 자기만의 커널이 없습니다. 호스트의 커널을 그대로 빌려 씁니다.** 이게 둘을 가르는 결정적 차이입니다.',
    },
    {
      type: 'table',
      headers: ['', '가상 머신(VM)', 'Docker 컨테이너'],
      rows: [
        ['OS', '앱마다 완전한 게스트 OS(커널까지)', '호스트 커널 공유, 앱+의존성만'],
        ['무게', '수 GB, 부팅 수십 초', '수백 MB 이하, 수 초 내 실행'],
        ['밀도', '한 서버에 소수', '한 서버에 수십~수백 개'],
        ['격리', '커널 수준까지 완전 분리', '상대적으로 얕음'],
      ],
    },
    {
      type: 'p',
      text: '그럼 커널을 공유하는데 격리는 누가 해줄까요? 커널 자신입니다. `namespace`는 프로세스가 보는 세계를 쪼개고(컨테이너 안에서 `ps`를 치면 자기 프로세스만 보이는 이유), `cgroups`는 CPU·메모리에 상한을 걸며, `OverlayFS`는 이미지를 레이어로 쌓아 공통 부분을 공유합니다. 즉 컨테이너의 격리는 마법이 아니라, **호스트 커널이 제공하는 기능들의 조합**입니다.',
    },
    {
      type: 'code',
      language: 'text',
      text: `[Docker 컨테이너]              [가상 머신]
  앱 A   앱 B                    앱 A        앱 B
   └───┬───┘                   게스트 커널  게스트 커널
   호스트 커널 (하나를 공유)         └────┬────┘
      하드웨어                     하이퍼바이저
                                    호스트 커널
                                    하드웨어`,
    },
    {
      type: 'image',
      src: '/blog/choi/04/vm-vs-container.svg',
      alt: 'VM과 Docker 컨테이너의 스택 구조 비교 — VM은 게스트마다 완전한 OS(커널)를 올리고, 컨테이너는 호스트 커널 하나를 공유한다',
      caption: 'VM은 게스트마다 OS를 통째로 올리지만, 컨테이너는 호스트 커널 하나를 나눠 쓴다.',
    },
    {
      type: 'quote',
      text: 'Docker의 가벼움도, 제약도 모두 "커널 하나를 여럿이 나눠 쓴다"는 단 하나의 선택에서 나옵니다.',
    },
    {
      type: 'p',
      text: '커널을 새로 올리지 않으니 부팅 과정이 없고, 그래서 컨테이너가 수 초 만에 뜹니다. 이게 가벼움의 정체입니다. 그런데 같은 뿌리에서 대가도 함께 자랍니다.',
    },
    { type: 'h3', text: '가벼움의 대가 — 배포 전날의 exec format error' },
    {
      type: 'p',
      text: '이 대가를 저는 배포 전날 몸으로 배웠습니다. 로컬에서 완벽히 뜨던 백엔드 컨테이너를 운영 서버에 올렸더니 시작하자마자 죽었고, 로그엔 딱 한 줄 `exec format error`만 남았습니다. 코드도 Dockerfile도 환경 변수도 로컬과 한 글자 다르지 않았는데 말이죠. 원인은 엉뚱한 곳에 있었습니다 — 제 노트북은 애플 실리콘(ARM), 운영 서버는 x86이었고, 저는 무심코 제 맥에서 빌드한 이미지를 그대로 올렸던 겁니다.',
    },
    {
      type: 'p',
      text: '컨테이너는 호스트 커널을 그대로 쓰기 때문에, 그 커널과 CPU 아키텍처에 묶입니다. "어디서든 똑같이"의 경계가 정확히 여기까지라는 걸 그제야 알았습니다. 커널 공유가 낳는 대가는 크게 셋입니다.',
    },
    {
      type: 'list',
      items: [
        '**CPU 아키텍처 종속** — x86(amd64)용 이미지는 ARM(애플 실리콘)에서 그대로 돌지 않습니다. 제 `exec format error`가 바로 이것. 해결은 운영 아키텍처에 맞춰 빌드하거나, 여러 아키텍처를 한 번에 굽는 것입니다(`docker buildx --platform linux/amd64,linux/arm64`).',
        '**커널 종속** — 리눅스 컨테이너는 리눅스 커널이 필요합니다. 그래서 맥·윈도우에서 Docker Desktop을 켜면 뒤에서 조용히 가벼운 리눅스 VM이 하나 돌고(맥은 가상화 프레임워크, 윈도우는 WSL2), 컨테이너는 그 안에서 실행됩니다. 그냥 되는 것처럼 보여도 실제로는 리눅스 위에서 도는 겁니다.',
        '**얕은 격리** — 모든 컨테이너가 같은 커널 하나를 공유하니, 커널에 취약점이 있으면 컨테이너 탈출(container escape)로 호스트나 옆 컨테이너까지 번질 수 있습니다. VM보다 "공유 지점"이 크다는 뜻이라, 신뢰할 수 없는 워크로드엔 gVisor·Kata Containers 같은 추가 격리를 얹습니다.',
      ],
    },
    {
      type: 'image',
      src: '/blog/choi/04/across-host-os.svg',
      alt: '호스트 OS별 Docker 실행 구조 — 리눅스에서는 컨테이너가 커널 위에서 바로 실행되고, 맥·윈도우에서는 숨겨진 리눅스 VM 안에서 실행된다',
      caption: '리눅스에선 커널 위에서 바로, 맥·윈도우에선 숨은 리눅스 VM 안에서 컨테이너가 돈다.',
    },

    { type: 'hr' },

    // ── Part 3 ──
    { type: 'h2', text: 'Part 3. 이미지를 작고 빠르게 — 레이어와 멀티 스테이지' },
    {
      type: 'p',
      text: '개념을 알았으니 실제로 이미지를 잘 만드는 이야기로 넘어가 봅시다. 처음 만든 백엔드 이미지는 약 780MB였습니다. JDK와 Gradle이 통째로 들어 있었거든요. 이미지가 크면 레지스트리에 올리고 서버가 내려받는 시간이 길어져 배포 한 번이 굼떠집니다. 이걸 330MB까지 줄인 과정에 Docker 이미지의 원리 두 가지가 그대로 담겨 있습니다.',
    },
    { type: 'h3', text: '레이어와 캐시 — 매번 다시 빌드해도 느리지 않은 이유' },
    {
      type: 'p',
      text: 'Dockerfile의 각 명령(`FROM`, `COPY`, `RUN` …)은 읽기 전용 **레이어** 하나가 됩니다. 이미지는 이 레이어를 쌓아올린 구조이고, Docker는 **바뀌지 않은 레이어를 캐시해서 재사용**합니다. 바뀐 레이어와 그 위쪽만 다시 만들죠. 그래서 "매번 전부 다시 빌드하면 느리지 않나?"의 답은 "아니다"입니다.',
    },
    {
      type: 'code',
      language: 'text',
      text: `┌─────────────────────────────────────┐
│  COPY app.jar   ← 코드 바뀜 → 재빌드 │  이 층만 새로 만듦
├─────────────────────────────────────┤
│  의존성 라이브러리        ← 캐시 재사용│
├─────────────────────────────────────┤
│  베이스 이미지(JRE)       ← 캐시 재사용│
└─────────────────────────────────────┘`,
    },
    {
      type: 'p',
      text: '실제로 백엔드 이미지를 처음부터 빌드하면 Gradle이 의존성을 내려받느라 **약 3분**이 걸립니다. 하지만 코드 한 줄만 고쳤을 땐 무거운 의존성 레이어가 캐시로 재사용되어 **약 15초**면 끝납니다. 여기서 규칙 하나가 나옵니다.',
    },
    {
      type: 'quote',
      text: '자주 바뀌는 것은 위에, 안 바뀌는 것은 아래에 — 이 한 줄이 Dockerfile 최적화의 절반입니다.',
    },
    {
      type: 'p',
      text: '스프링에선 한 걸음 더 갈 수 있습니다. 부트가 만드는 실행 JAR은 의존성까지 하나로 뭉친 "뚱뚱한 JAR"이라, 그대로 `COPY`하면 코드 한 줄에도 수백 MB 레이어를 통째로 다시 만듭니다. **레이어드 JAR(layered jar)** 로 "잘 안 바뀌는 의존성"과 "자주 바뀌는 내 코드"를 나눠 서로 다른 `COPY` 레이어에 넣으면, 코드만 고쳤을 때 의존성 레이어 캐시가 그대로 살아남습니다.',
    },
    { type: 'h3', text: '멀티 스테이지 빌드 — 빌드 도구를 최종 이미지에 남기지 않는다' },
    {
      type: 'p',
      text: '나머지 절반은 크기입니다. 빌드에는 JDK와 Gradle이 필요하지만, **실행에는 JRE와 JAR만 있으면 됩니다.** 멀티 스테이지 빌드는 빌드 단계와 실행 단계를 나눠, 무거운 빌드 도구를 최종 이미지에서 버립니다.',
    },
    {
      type: 'code',
      language: 'dockerfile',
      text: `# 빌드 단계 — 최종 이미지에 포함되지 않고 버려짐
FROM eclipse-temurin:21-jdk AS build
WORKDIR /app
COPY . .
RUN ./gradlew clean bootJar -x test

# 실행 단계 — JAR만 복사해 가져옴
FROM eclipse-temurin:21-jre
WORKDIR /app
COPY --from=build /app/build/libs/*.jar app.jar
EXPOSE 8080
ENTRYPOINT ["java", "-jar", "app.jar"]`,
    },
    {
      type: 'table',
      headers: ['빌드 방식', '베이스 이미지', '최종 크기'],
      rows: [
        ['단일 스테이지 (JDK 포함)', 'temurin:21-jdk', '약 780MB'],
        ['멀티 스테이지 (JRE)', 'temurin:21-jre', '약 330MB'],
      ],
    },
    {
      type: 'p',
      text: 'JDK를 버린 것만으로 이미지가 절반 이하로 줄었습니다. `-alpine`이나 distroless 같은 초경량 베이스로 더 줄일 수도 있지만, alpine은 표준과 다른 C 라이브러리(musl)를 써서 드물게 네이티브 라이브러리가 말썽을 부립니다. 자바는 무난하게 `-jre` 계열로 시작하길 권합니다. 프론트엔드도 원리는 같습니다 — Node로 빌드해 정적 파일(`/dist`)만 만든 뒤 가벼운 nginx 이미지로 서빙하고, Node 환경은 버립니다.',
    },
    {
      type: 'p',
      text: '작은 이야기 하나 더. 이미지 태그의 `latest`는 "항상 최신"이라는 약속이 아니라 그냥 기본 태그 이름일 뿐이라, 언제든 다른 내용을 가리킬 수 있습니다. **운영에서는 `backend:1.4.2`처럼 버전을 못 박아** 장애 시 어느 이미지로 롤백할지 추적할 수 있게 합니다.',
    },

    { type: 'hr' },

    // ── Part 4 ──
    { type: 'h2', text: 'Part 4. 굴리다 보면 만나는 것들 — 세 가지 함정 이야기' },
    {
      type: 'p',
      text: '이미지를 만들었다고 끝이 아닙니다. 로컬에서 잘 돌던 컨테이너가 운영에서 조용히 어긋나는 지점들이 있었습니다. 제가 실제로 밟은 세 가지를 이야기로 풀어봅니다.',
    },
    { type: 'h3', text: '① 설정은 이미지에 굽지 말고 주입한다' },
    {
      type: 'p',
      text: '로컬과 운영은 DB 주소도, 프로파일도, 로그 레벨도 다릅니다. 이 값을 이미지에 하드코딩하면 환경마다 이미지를 따로 구워야 하고, 그 순간 "한 이미지를 어디서든"이라는 전제가 깨집니다. 그래서 설정은 **실행 시점에 환경 변수로 주입**합니다. 스프링은 `SPRING_DATASOURCE_URL` 같은 환경 변수를 별도 설정 없이 자동으로 읽습니다.',
    },
    {
      type: 'p',
      text: "다만 **비밀번호·API 키는 절대 이미지에 넣지 않습니다.** 이미지 레이어 히스토리에 영구히 박혀, 나중에 지워도 흔적이 남기 때문입니다. Compose에서는 `.env` 파일에 담고 `${DB_PASSWORD}` 형태로 참조하며, `.env`는 `.gitignore`에 넣어 저장소에 올리지 않습니다.",
    },
    { type: 'h3', text: '② 바인드 마운트의 inode 함정' },
    {
      type: 'p',
      text: '개발 중에는 이미지를 매번 다시 굽지 않고 호스트의 JAR을 컨테이너에 연결해 쓰고 싶어집니다. 이때 쓰는 게 바인드 마운트입니다. 볼륨과 헷갈리기 쉬운데, **호스트 경로를 주면 바인드 마운트, 이름만 주면 볼륨**입니다.',
    },
    {
      type: 'table',
      headers: ['', '바인드 마운트', '볼륨(named volume)'],
      rows: [
        ['위치 결정', '내가 호스트 경로 직접 지정', 'Docker가 알아서 관리'],
        ['주요 용도', '개발 중 JAR·소스 실시간 반영', 'DB 데이터 등 영속 데이터'],
        ['문법', '`-v ./app.jar:/app.jar`', '`-v db-data:/var/lib/postgresql/data`'],
      ],
    },
    {
      type: 'p',
      text: '여기서 한 번 넘어졌습니다. JAR 파일 하나만 바인드 마운트해 뒀는데, 새로 빌드한 JAR이 컨테이너에 반영되지 않았습니다. 원인은 **inode**였습니다. 빌드 도구가 JAR을 "덮어쓰기"가 아니라 "지우고 새로 생성"하면 파일의 실체(inode)가 바뀌는데, 단일 파일 마운트는 옛 inode를 계속 가리킵니다. **JAR이 든 폴더를 통째로 마운트**하면 해결됩니다. (그리고 실행 중인 JVM은 옛 JAR을 메모리에 올려둔 상태라, 파일을 갈아도 컨테이너를 재시작해야 반영됩니다.)',
    },
    { type: 'h3', text: '③ JVM은 컨테이너 메모리 상한을 모른다' },
    {
      type: 'p',
      text: '가장 당황스러웠던 함정입니다. 백엔드 컨테이너에 `--memory=512m`을 걸었더니, 부팅 직후 이유도 없이 강제 종료(OOM Kill)됐습니다. 원인은 JVM과 커널의 엇갈림이었습니다. 이 메모리 상한은 커널의 `cgroups` 기능인데(Part 2에서 나온 그 cgroups입니다), **옛 JVM은 이 값을 못 보고 호스트 전체 메모리 기준으로 힙을 잡다가** 상한을 넘겨 죽은 겁니다.',
    },
    {
      type: 'p',
      text: 'Java 10 이후(8도 최신 패치)는 이 cgroups 값을 읽는 "컨테이너 인식"이 기본이라 대개는 괜찮습니다. 그래도 힙 비율을 명시적으로 지정하는 게 안전합니다.',
    },
    {
      type: 'code',
      language: 'dockerfile',
      text: `ENTRYPOINT ["java", "-XX:MaxRAMPercentage=75.0", "-jar", "app.jar"]
# 컨테이너 메모리 상한의 75%를 힙으로 사용
# 나머지 25%는 메타스페이스·스레드 스택·GC 몫으로 남긴다`,
    },

    { type: 'hr' },

    // ── Part 5 ──
    { type: 'h2', text: 'Part 5. 다섯 서비스를 한 줄로 — docker-compose.yml' },
    {
      type: 'p',
      text: '이제 조각을 합칠 차례입니다. I-Poten 스택은 다섯 컨테이너입니다. React 프론트엔드(nginx 서빙), Spring Boot 백엔드, Python FastAPI AI 서비스, PostgreSQL DB, 그리고 이들 앞에 앉아 외부 요청을 받는 nginx 리버스 프록시. 이걸 매번 `docker run` 다섯 번으로 띄우는 대신, **무엇이 어떻게 연결되는지를 한 파일에 선언**합니다.',
    },
    {
      type: 'code',
      language: 'text',
      text: `                    사용자 브라우저
                           │  :80
                           ▼
                   ┌──────────────────┐
                   │   proxy (nginx)   │  ← 외부에 공개되는 유일한 문
                   └──────┬──────┬────┘
            "/"           │      │  "/api/*"
                          ▼      ▼
              ┌──────────────┐  ┌──────────────────┐
              │   frontend    │  │     backend       │
              │  정적/nginx   │  │  Spring :8080     │
              └──────────────┘  └───┬──────────┬────┘
                           "/predict"│          │ JDBC
                                     ▼          ▼
                          ┌──────────────┐  ┌──────────────────┐
                          │  ai (FastAPI) │  │  db (PostgreSQL)  │
                          │    :8000      │  │      :5432        │
                          └──────────────┘  └──────┬───────────┘
                                                   │ volume
                                            [ db-data 영속 저장 ]`,
    },
    {
      type: 'p',
      text: '이 한 파일이 만들어내는 동작 네 가지가 Compose의 값어치를 그대로 보여줍니다.',
    },
    { type: 'h3', text: '① 서비스 이름이 곧 내부 주소다' },
    {
      type: 'p',
      text: 'Compose는 모든 서비스를 같은 내부 네트워크에 자동으로 연결합니다. 이 안에서는 **IP도 `localhost`도 아닌 서비스 이름이 호스트명 역할**을 합니다. 백엔드는 DB를 `db:5432`로, AI를 `http://ai:8000`으로 부릅니다. IP를 외울 일도, 컨테이너가 재시작되며 주소가 바뀔 걱정도 없습니다.',
    },
    {
      type: 'code',
      language: 'yaml',
      text: `backend:
  build: ./backend
  environment:
    SPRING_DATASOURCE_URL: jdbc:postgresql://db:5432/appdb
    AI_SERVICE_URL: http://ai:8000   # 서비스 이름이 곧 주소
  depends_on:
    db:
      condition: service_healthy     # DB가 준비된 뒤에 기동`,
    },
    { type: 'h3', text: '② 외부 포트는 프록시만 연다' },
    {
      type: 'p',
      text: '`ports`로 호스트에 포트를 여는 건 프록시 하나뿐입니다. 프론트·백엔드·AI·DB는 내부 네트워크에서만 서로 부릅니다. 바깥에서 DB 포트에 직접 닿을 방법이 아예 없다는 뜻 — **공격 표면을 최소화하는 것이 이 구조의 보안 전략**입니다.',
    },
    {
      type: 'code',
      language: 'nginx',
      text: `# proxy/nginx.conf — 외부의 유일한 입구
server {
  listen 80;
  location /api/ { proxy_pass http://backend:8080/; }
  location /     { proxy_pass http://frontend:80/; }
}`,
    },
    { type: 'h3', text: '③ 기동 순서를 헬스체크로 통제한다' },
    {
      type: 'p',
      text: '백엔드가 DB보다 먼저 뜨면 연결 실패로 크래시합니다. `depends_on`에 `healthcheck`를 붙여, PostgreSQL이 `pg_isready`를 통과한 뒤에야 백엔드가 기동되도록 합니다. "순서대로 뜬다"를 사람이 챙기는 게 아니라 파일이 보장하는 거죠.',
    },
    {
      type: 'code',
      language: 'yaml',
      text: `db:
  image: postgres:16
  volumes:
    - db-data:/var/lib/postgresql/data   # 컨테이너를 지워도 데이터는 산다
  healthcheck:
    test: ["CMD-SHELL", "pg_isready -U app"]
    interval: 5s
    retries: 5`,
    },
    { type: 'h3', text: '④ 프론트 환경 변수는 빌드 시점에 굳는다' },
    {
      type: 'p',
      text: '마지막 함정 하나. React(Vite) 같은 프론트엔드는 API 주소 같은 값이 **빌드 시점에 JS 번들로 굳습니다.** 백엔드처럼 실행 시 `-e`로 바꾸는 게 아니라, 빌드할 때 `ARG`/`--build-arg`로 넣어야 합니다. 이걸 놓치면 운영 빌드가 `localhost`를 바라보는 사고가 납니다.',
    },
    {
      type: 'code',
      language: 'dockerfile',
      text: `# frontend/Dockerfile
FROM node:20 AS build
ARG VITE_API_URL
ENV VITE_API_URL=$VITE_API_URL    # 빌드 시점에 JS 코드로 굳음
RUN npm run build

FROM nginx:1.27-alpine
COPY --from=build /app/dist /usr/share/nginx/html`,
    },
    {
      type: 'p',
      text: '이제 `docker compose up --build` 한 줄이면 다섯 이미지가 빌드되고, 내부 네트워크와 볼륨이 만들어지고, 올바른 순서로 전부 뜹니다. 브라우저에서 `http://localhost` 하나로 앱 전체가 동작합니다. 글머리에서 말한 "명령 한 줄"이 바로 이겁니다.',
    },

    { type: 'hr' },

    // ── Part 6 ──
    { type: 'h2', text: 'Part 6. 그런데, 꼭 Docker여야 할까 — 직접 설치와 비교' },
    {
      type: 'p',
      text: '여기까지 오면 Docker가 만능처럼 보이지만, 그렇지는 않습니다. 호스트에 프로그램을 바로 까는 **직접 설치**도 여전히 유효한 선택이고, 어떤 경우엔 더 낫습니다. 무엇을 배포하느냐에 따라 답이 갈리는데, 특히 **앱과 DB는 성격이 완전히 다릅니다.**',
    },
    { type: 'h3', text: '앱(웹·API) 배포 — 대체로 Docker의 손을 들어준다' },
    {
      type: 'p',
      text: '먼저 상태를 갖지 않는 앱 서비스(웹 서버, REST API 등)를 봅시다. 이 영역에서는 대부분 Docker가 이깁니다.',
    },
    {
      type: 'table',
      headers: ['관점', '직접 설치 (호스트에 바로)', 'Docker'],
      rows: [
        ['환경 재현', '문서에 의존 → 시간이 지나면 휘발', '이미지로 코드화 → 어디서든 동일'],
        ['의존성 격리', '앱끼리 라이브러리 버전 충돌 가능', '컨테이너마다 독립'],
        ['배포·롤백', '수동, 이전 상태 복구가 번거로움', '이미지 태그로 즉시 롤백'],
        ['성능', '오버헤드 없음(네이티브)', '얇은 추상화 계층(대개 무시할 수준)'],
        ['학습·운영', '익숙하면 단순', 'Docker 개념·도구 학습 필요'],
      ],
    },
    {
      type: 'p',
      text: '상태가 없는 앱은 언제든 죽여도, 새로 띄워도 잃을 게 없습니다. 컨테이너의 "일회용" 철학과 완벽히 맞아떨어지죠. 재현성·격리·즉시 롤백의 가치가 얇은 오버헤드를 압도합니다. I-Poten의 프론트·백엔드·AI 서비스를 전부 컨테이너로 둔 이유입니다.',
    },
    { type: 'h3', text: 'DB는 이야기가 다르다 — 상태(state)라는 변수' },
    {
      type: 'p',
      text: '데이터베이스는 정반대입니다. DB는 **상태를 갖습니다(stateful).** 데이터는 컨테이너가 죽어도 반드시 살아남아야 하고, 이건 "언제든 버리고 새로 띄운다"는 컨테이너의 철학과 정면으로 부딪힙니다.',
    },
    {
      type: 'quote',
      text: '컨테이너의 철학은 "언제든 버리고 새로 띄운다"입니다. 그래서 버리면 안 되는 데이터와는 근본적으로 긴장 관계에 있습니다.',
    },
    {
      type: 'p',
      text: '물론 볼륨(named volume)으로 데이터를 컨테이너 바깥에 영속시킬 수 있습니다. 하지만 운영 규모가 커지면 백업·복구, 고가용성, 디스크 I/O 성능 같은 요구가 커지고, 이걸 컨테이너 위에서 직접 감당하는 건 부담이 됩니다. 그래서 DB는 상황에 따라 답이 달라집니다.',
    },
    {
      type: 'table',
      headers: ['상황', '권장', '이유'],
      rows: [
        ['개발·테스트', 'Docker DB', '일회용, 버전 전환·초기화가 자유롭다'],
        ['소규모 운영(서버 1대)', 'Docker DB + 볼륨', '구성이 단순하고, 데이터는 볼륨으로 영속'],
        ['규모 있는 운영', '관리형 DB(RDS 등)·전용 호스트', '백업·복구·고가용성·I/O를 위탁·전담'],
      ],
    },
    {
      type: 'p',
      text: '**I-Poten의 선택은 정직하게 밝히면 "지금은 Docker, 나중엔 분리"입니다.** 아직 단일 서버라 DB도 컨테이너로 두되 `db-data` 볼륨으로 데이터를 영속시켰습니다. 구성이 단순해지는 이득이 크니까요. 다만 트래픽이 커지면 DB만 떼어 관리형 서비스로 옮길 계획입니다. 앱은 컨테이너로, DB는 규모에 맞춰 — 이게 지금 이 맥락에서 내린 최선입니다.',
    },

    { type: 'hr' },

    // ── Part 7 ──
    { type: 'h2', text: 'Part 7. 실전에서 챙겨야 할 것들 — 설정과 주의사항' },
    {
      type: 'p',
      text: '앞의 함정들이 개념을 몰라 밟은 것이라면, 마지막으로 정리할 건 습관에 가깝습니다. 실제로 Docker를 운영하며 "미리 알았으면 좋았을" 설정과 주의점을 모았습니다. 하나씩 큰 노력이 드는 건 아니지만, 빠뜨리면 꼭 나중에 대가를 치릅니다.',
    },
    { type: 'h3', text: '① 빌드 컨텍스트는 .dockerignore로 줄인다' },
    {
      type: 'p',
      text: '`docker build`는 현재 폴더 전체를 "빌드 컨텍스트"로 데몬에 통째로 넘깁니다. `node_modules`나 `.git`이 딸려 들어가면 빌드가 느려지고 이미지가 커지며, 최악의 경우 `.env` 같은 시크릿까지 컨텍스트에 섞여 들어갑니다. `.gitignore`처럼 `.dockerignore`로 미리 걸러내세요.',
    },
    {
      type: 'code',
      language: 'text',
      text: `# .dockerignore
node_modules
.git
.env
build
dist`,
    },
    { type: 'h3', text: '② 컨테이너를 root로 돌리지 않는다' },
    {
      type: 'p',
      text: 'Dockerfile은 기본적으로 프로세스를 root로 실행합니다. 커널을 공유하는 구조(Part 2)라, 컨테이너 안 root가 호스트에 미치는 위험이 VM보다 큽니다. `USER`로 일반 사용자를 지정하세요. 더해서 이미지는 Trivy·docker scout 같은 도구로 취약점을 스캔하는 습관을 들이면 좋습니다.',
    },
    {
      type: 'code',
      language: 'dockerfile',
      text: `RUN useradd -r appuser        # 권한 없는 일반 사용자 생성
USER appuser                 # 이후 프로세스는 이 사용자로 실행
ENTRYPOINT ["java", "-jar", "app.jar"]`,
    },
    { type: 'h3', text: '③ 자동 재시작과 헬스체크로 스스로 낫게 한다' },
    {
      type: 'p',
      text: '서버가 재부팅되거나 컨테이너가 크래시해도 다시 뜨도록 `restart` 정책을 겁니다. 여기에 `HEALTHCHECK`를 더하면 "떠 있지만 실은 응답 못 하는" 상태까지 감지할 수 있습니다. 앞서 DB에서 봤듯, Compose는 이 헬스체크 값으로 서비스 상태를 판단합니다.',
    },
    {
      type: 'code',
      language: 'yaml',
      text: `backend:
  restart: unless-stopped          # 재부팅·크래시 후 자동 복구
  healthcheck:
    test: ["CMD", "curl", "-f", "http://localhost:8080/actuator/health"]
    interval: 10s
    retries: 3`,
    },
    { type: 'h3', text: '④ graceful shutdown을 막지 않는다 — PID 1 함정' },
    {
      type: 'p',
      text: '의외로 많이 놓치는 부분입니다. `ENTRYPOINT`를 셸 형식(`ENTRYPOINT java -jar app.jar`)으로 쓰면 셸이 PID 1이 되고, `docker stop`이 보내는 종료 신호(SIGTERM)를 자바에 전달하지 않습니다. 결과적으로 앱이 정리할 틈도 없이 10초 뒤 강제 종료되죠. **exec 형식(JSON 배열)** 으로 쓰면 자바가 직접 PID 1이 되어 신호를 받고, 진행 중인 요청을 마무리한 뒤 깔끔하게 내려갑니다.',
    },
    {
      type: 'code',
      language: 'dockerfile',
      text: `# 나쁨: 셸이 PID 1 → SIGTERM이 자바까지 전달되지 않음
ENTRYPOINT java -jar app.jar

# 좋음: 자바가 PID 1 → SIGTERM 직접 수신 → 정상 종료(graceful shutdown)
ENTRYPOINT ["java", "-jar", "app.jar"]`,
    },
    { type: 'h3', text: '⑤ 디스크는 조용히 찬다' },
    {
      type: 'p',
      text: '빌드를 반복하면 태그 없는 옛 이미지(dangling image), 멈춘 컨테이너, 안 쓰는 볼륨이 쌓여 디스크를 잠식합니다. 가끔 `docker system prune`으로 정리하세요. 단 `--volumes` 옵션은 볼륨(=데이터)까지 지우므로, DB 볼륨이 물려 있다면 각별히 주의해야 합니다.',
    },
    {
      type: 'code',
      language: 'bash',
      text: `docker system prune              # 안 쓰는 이미지·컨테이너·네트워크 정리
docker system prune --volumes    # 볼륨까지 삭제 — 데이터 날아갈 수 있음, 주의!`,
    },
    { type: 'h3', text: '자주 쓰는 디버깅 명령어 (참고)' },
    {
      type: 'p',
      text: '마지막으로, 컨테이너가 이상할 때 가장 자주 꺼내 쓰는 명령들을 모아 둡니다. 대부분의 문제는 이 다섯 줄 안에서 실마리가 잡힙니다.',
    },
    {
      type: 'code',
      language: 'bash',
      text: `docker logs -f <컨테이너>       # 로그 실시간으로 보기
docker exec -it <컨테이너> sh    # 컨테이너 안으로 들어가 셸 열기
docker ps                       # 실행 중인 컨테이너 목록
docker inspect <컨테이너>       # 네트워크·마운트 등 상세 정보
docker stats                    # 실시간 자원(CPU·메모리) 사용량`,
    },

    { type: 'hr' },

    // ── 전체 조망 · 한계 ──
    { type: 'h2', text: '한 대의 서버, 그리고 그다음' },
    {
      type: 'p',
      text: '정직하게 짚고 넘어갈 것이 있습니다. 지금 구성은 **한 대의 서버에서 Compose로 돌리는 그림**이고, 이 그림이 감당하지 못하는 요구가 분명히 있습니다.',
    },
    {
      type: 'table',
      headers: ['요구', '지금 (Compose · 서버 1대)', '넘어가야 할 시점 (Kubernetes)'],
      rows: [
        ['서비스 확장', '수동으로 컨테이너 수 조정', '부하 따라 자동 스케일'],
        ['무중단 배포', 'down→up 사이 순단 발생', '롤링 업데이트로 순단 최소화'],
        ['장애 복구', 'restart 정책으로 재기동', '노드가 죽어도 다른 노드에 재배치'],
        ['여러 대 서버', '불가 (단일 호스트)', '클러스터로 묶어 오케스트레이션'],
      ],
    },
    {
      type: 'p',
      text: '트래픽이 커져 서버가 여러 대가 되고 이런 요구가 현실이 되면, 그때가 **Kubernetes**로 넘어갈 시점입니다. 중요한 건, 지금 만든 **이미지와 서비스 분리 구조는 그대로 재사용**된다는 점입니다. Kubernetes는 그 이미지들을 더 똑똑하게 배치·관리해줄 뿐, 우리가 지금 다진 기반을 버리게 하지 않습니다.',
    },

    { type: 'hr' },

    // ── 마치며 ──
    { type: 'h2', text: '마치며 — 마법 상자를 열어보고 나서' },
    {
      type: 'quote',
      text: 'Docker를 쓴다는 건 "환경도 코드로 관리하겠다"는 약속입니다. 그 약속을 지킬 때, 배포는 마침내 예측 가능해집니다.',
    },
    {
      type: 'p',
      text: '기초부터 다시 짚고 나니, 오래 마법 상자로만 보이던 Docker가 이해할 수 있는 구조로 바뀌었습니다. 컨테이너는 커널을 나눠 쓰는 가벼운 실행 단위이고, 이미지는 환경을 굳힌 설계도이며, 그 가벼움은 커널·아키텍처라는 경계와 맞바꾼 것이었습니다. `exec format error`도, OOM Kill도, 알고 나면 더는 신기한 일이 아니라 다룰 수 있는 대상이 됐습니다.',
    },
    {
      type: 'p',
      text: '이번 작업에서 가장 크게 남은 건 **이미지는 문서보다 정확하다**는 감각입니다. README의 설치 안내는 시간이 지나면 낡지만, Dockerfile은 실행되지 않으면 곧바로 틀렸음이 드러납니다. 환경 설정의 진실은 Dockerfile에 있습니다.',
    },
    {
      type: 'p',
      text: '부족한 글 읽어주셔서 감사합니다. 더 나은 방법이나 피드백은 언제든 환영합니다.',
    },
  ],
};
